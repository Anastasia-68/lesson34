# 🚀 Webpack Asset Management Project

Учебный проект по настройке Webpack для управления различными типами активов (assets). Выполнен в рамках курса "Web-разработка".

## 🛠 Реализованные возможности:

* **Хеширование файлов**: Имена выходных файлов (`bundle.[contenthash].js`) содержат уникальный хеш для управления кэшированием в браузере.
* **Локальные шрифты**: Подключен шрифт Roboto через `@font-face` (обработка `.ttf` файлов).
* **Работа с изображениями**: Настроена загрузка и оптимизация картинок (Asset Modules).
* **Стили CSS**: Интеграция стилей с использованием `style-loader` и `css-loader`.
* **Оптимизация**: Использование `splitChunks` для отделения кода внешних библиотек (vendors) от основного кода приложения.

## 🚀 Как запустить проект

1. Установите зависимости:
   ```bash
   npm install

2.Запустите режим разработки (локальный сервер):
   ```bash
   npm start

3.Соберите проект для продакшена:
  ```bash
   npm run build
